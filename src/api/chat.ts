// src/api/chat.ts
export async function drawCardChat(cardId: string, question: string) {
    // TODO: hit FastAPI /chat
    // For now, return a mock string.
    return `The meaning of card ${cardId} is a work‑in‑progress.`;
  }