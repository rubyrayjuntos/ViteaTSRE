// src/api/voice.ts
export async function speakReading(text: string) {
    // TODO: call your ad‑hoc voice endpoint
    // Stub: log to console and resolve immediately
    console.log('[voice] →', text);
  }